<h1 class="text-center flex-grow-1 mb-0">Quản lý Món ăn & Thức uống</h1>

<!-- Nút điều hướng -->
<div class="mb-3">
    <%= link_to "Quay lại", admin_dashboard_path, class: "btn btn-secondary me-2" %>
    <%= link_to "Tất cả", admin_food_drinks_path, class: "btn btn-secondary me-2" %>
    <% Category.all.each do |category| %>
    <%= link_to category.name, admin_food_drinks_path(category_id: category.id), class: "btn btn-primary me-2" %>
    <% end %>
</div>

<!-- Flash messages -->
<%= turbo_frame_tag "flash_messages" do %>
<%= render "shared/flash" %>
<% end %>

<!-- Thanh công cụ -->
<div class="d-flex justify-content-between align-items-center mb-3 flex-wrap">
    <div class="mb-2">
        <%= link_to "Thêm mới", new_admin_food_drink_path, class: "btn btn-success me-2" %>
        <%= link_to "Làm mới", admin_food_drinks_path, class: "btn btn-outline-secondary" %>
    </div>

    <!-- Form tìm kiếm & filter với Turbo Frame -->
    <div class="d-flex align-items-center mb-2">
        <%= form_with url: admin_food_drinks_path, method: :get, local: false, data: { turbo_frame: "food_drinks_list" }, class: "d-flex" do |f| %>
        <%= f.text_field :query, value: params[:query], placeholder: "🔍 Tìm kiếm món ăn hoặc thức uống...", class: "form-control me-2" %>
        <%= f.select :sort, options_for_select([
        ['Mới nhất', 'newest'],
        ['Tên A → Z', 'name_asc'],
        ['Tên Z → A', 'name_desc'],
        ['Giá tăng dần', 'price_asc'],
        ['Giá giảm dần', 'price_desc']
      ], params[:sort]), {}, class: "form-select me-2" %>
        <%= f.submit "Lọc", class: "btn btn-primary" %>
        <% end %>
    </div>
</div>

<!-- Nút chọn tất cả -->
<div class="mb-3">
    <button type="button" id="select_all_btn" class="btn btn-warning me-2">Chọn tất cả món ăn</button>
</div>

<!-- Turbo Frame chứa bảng món ăn -->
<%= turbo_frame_tag "food_drinks_list" do %>
<%= form_with url: bulk_delete_admin_food_drinks_path, method: :delete, local: false, data: { turbo_confirm: "Bạn có chắc chắn muốn xóa các món đã chọn?" } do %>
<%= render partial: "list", locals: { food_drinks: @food_drinks } %>
<div class="mb-3">
    <%= submit_tag "Xóa các món đã chọn", class: "btn btn-danger" %>
</div>
<% end %>
<% end %>

<% if @food_drinks.empty? %>
<p class="text-center text-muted mt-3">Không tìm thấy món ăn/thức uống nào phù hợp.</p>
<% end %>

<!-- JS chọn/bỏ chọn tất cả + Auto search -->
<script>
    document.addEventListener("turbo:load", () => {
        // Chọn / bỏ chọn tất cả
        const selectAllBtn = document.querySelector("#select_all_btn");
        if (selectAllBtn) {
            let allSelected = false;
            selectAllBtn.addEventListener("click", () => {
                allSelected = !allSelected;
                document.querySelectorAll(".food-drink-checkbox").forEach(cb => cb.checked = allSelected);
                selectAllBtn.textContent = allSelected ? "Bỏ chọn tất cả" : "Chọn tất cả món ăn";
            });
        }

        // Auto search
        const searchInput = document.querySelector('input[name="query"]');
        if (searchInput) {
            let timeout = null;
            searchInput.addEventListener("input", () => {
                clearTimeout(timeout);
                timeout = setTimeout(() => {
                    const form = searchInput.closest("form");
                    form.requestSubmit(); // submit form bằng Turbo, update frame food_drinks_list
                }, 300); // 300ms debounce
            });
        }
    });
</script>