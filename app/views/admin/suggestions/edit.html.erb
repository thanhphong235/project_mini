<div class="suggestion-detail">

    <h2>Cập nhật trạng thái góp ý</h2>

    <div class="detail-row">
        <span class="label">Người gửi:</span>
        <span class="value"><%= @suggestion.user.name || @suggestion.user.email %></span>
    </div>

    <div class="detail-row">
        <span class="label">Tiêu đề:</span>
        <span class="value"><%= @suggestion.title %></span>
    </div>

    <div class="detail-row">
        <span class="label">Nội dung:</span>
        <span class="value"><%= simple_format(@suggestion.content) %></span>
    </div>

    <hr>

    <%= form_with model: [:admin, @suggestion], local: true, html: { class: "update-status-form" } do |f| %>
    <div class="detail-row">
        <span class="label">Trạng thái:</span>
        <span class="value">
            <%= f.select :status, [["Chờ duyệt", "pending"], ["Chấp nhận", "approved"], ["Từ chối", "rejected"]],
          {}, class: "form-select" %>
        </span>
    </div>

    <div class="detail-actions mt-3">
        <%= f.submit "Lưu", class: "btn btn-success me-2" %>
        <%= link_to "Quay lại", admin_suggestions_path, class: "btn btn-secondary" %>
    </div>
    <% end %>

</div>