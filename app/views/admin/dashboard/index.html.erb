<h1 class="mb-4 text-center text-primary">🛠️ Trang Quản Trị</h1>

<p class="text-center lead">
    Chào <strong><%= current_user.name || current_user.email %></strong>!
    Bạn đang đăng nhập với quyền:
    <span class="badge bg-<%= current_user.admin? ? 'danger' : 'secondary' %>">
        <%= current_user.admin? ? "Admin" : "User" %>
    </span>
</p>

<hr class="my-4">

<div class="row g-4 justify-content-center">
    <% admin_sections = [
    {title: "Người dùng", text: "Quản lý tất cả người dùng của hệ thống", path: admin_users_path, icon: "bi-people"},
    {title: "Danh mục", text: "Quản lý tất cả danh mục sản phẩm", path: admin_categories_path, icon: "bi-list-ul"},
    {title: "Sản phẩm", text: "Quản lý tất cả món ăn & thức uống, kèm hình ảnh", path: admin_food_drinks_path, icon: "bi-basket"},
    {title: "Đơn hàng", text: "Xem và quản lý tất cả đơn hàng của khách", path: admin_orders_path, icon: "bi-receipt"},
    {title: "Góp ý", text: "Xem, duyệt và quản lý các góp ý từ người dùng", path: admin_suggestions_path, icon: "bi-chat-left-text"},
    {title: "Thống kê đơn hàng", text: "Xem thống kê đơn hàng tính đến hôm nay", path: admin_order_statistics_path, icon: "bi-bar-chart"}
  ] %>

    <% admin_sections.each do |section| %>
    <div class="col-sm-6 col-md-4 col-lg-3">
        <div class="card h-100 shadow-sm border-0 hover-shadow">
            <div class="card-body text-center">
                <i class="bi <%= section[:icon] %> display-4 text-primary mb-3"></i>
                <h5 class="card-title mb-2"><%= section[:title] %></h5>
                <p class="card-text text-muted mb-3"><%= section[:text] %></p>
                <%= link_to section[:title], section[:path], class: "btn btn-outline-primary w-100" %>
            </div>
        </div>
    </div>
    <% end %>
</div>