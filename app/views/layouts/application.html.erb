<!DOCTYPE html>
<html>

<head>
    <title>Foods & Drinks App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">

</head>

<body class="d-flex flex-column min-vh-100">
    <!-- Header -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm fixed-top">
            <div class="container">
                <%= link_to "Foods & Drinks App", root_path, class: "navbar-brand" %>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto align-items-center">
                        <% if user_signed_in? %>
                        <% if current_user.admin? %>
                        <!-- ADMIN -->
                        <li class="nav-item">
                            <%= link_to "Dashboard", admin_dashboard_path, class: "nav-link" %>
                        </li>
                        <% else %>
                        <!-- USER -->
                        <li class="nav-item ms-3">
                            <%= turbo_frame_tag "cart-count" do %>
                            <%= render "shared/cart_icon" %>
                            <% end %>
                        </li>
                        <% end %>

                        <!-- Dropdown tài khoản -->
                        <li class="nav-item dropdown ms-3">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-person-circle me-1"></i> <%= current_user.name || "Tài khoản" %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li><%= link_to "Profile", edit_user_registration_path, class: "dropdown-item" %></li>
                                <% unless current_user.admin? %>
                                <li><%= link_to "Lịch sử đơn hàng", orders_path, class: "dropdown-item" %></li>
                                <li><%= link_to "Góp ý của tôi", suggestions_path, class: "dropdown-item" %></li>
                                <li><%= link_to "Shopping", food_drinks_path, class: "dropdown-item" %></li>
                                <% end %>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li>
                                    <%= link_to "Đăng xuất", destroy_user_session_path,
                                                    data: { turbo_method: :delete, turbo_confirm: "Bạn chắc chắn muốn đăng xuất?" },
                                                    class: "dropdown-item text-danger" %>
                                </li>
                            </ul>
                        </li>
                        <% else %>
                        <!-- CHƯA ĐĂNG NHẬP -->
                        <li class="nav-item">
                            <%= link_to "Log in", new_user_session_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item ms-2">
                            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
                        </li>
                        <% end %>
                    </ul>
                </div>

            </div>
        </nav>
    </header>

    <!-- Main -->
    <main class="flex-grow-1" style="padding-top: 80px;">
        <div class="container mt-4">
            <!-- Flash messages -->
            <%
=begin%>
            <% flash.each do |key, message| %>
            <% alert_class =
      case key
      when "notice" then "success"   # xanh lá cho thành công
      when "alert"  then "warning"   # vàng cho cảnh báo
      when "error"  then "danger"    # đỏ cho lỗi
      else "info"                   # mặc định xanh dương
      end
  %>

            <div class="alert alert-<%= alert_class %> alert-dismissible fade show" role="alert">
                <%= message %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            <% end %>
            <%
=end%>

            <%= yield %>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-light py-3 mt-auto">
        <div class="container">
            <div class="row text-center text-md-start">
                <!-- Cột 1: Giới thiệu -->
                <div class="col-md-4 mb-4">
                    <h5 class="fw-bold mb-3">🍴 Foods & Drinks App</h5>
                    <p class="text-secondary">
                        Ứng dụng giúp bạn khám phá, đặt món ăn và đồ uống yêu thích mọi lúc, mọi nơi.
                    </p>
                </div>

                <!-- Cột 2: Liên kết nhanh -->
                <div class="col-md-4 mb-4">
                    <h6 class="fw-bold mb-3 text-uppercase">Liên kết nhanh</h6>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Trang chủ</a></li>
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Về chúng tôi</a></li>
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Chính sách bảo mật</a></li>
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Điều khoản sử dụng</a></li>
                    </ul>
                </div>

                <!-- Cột 3: Mạng xã hội -->
                <div class="col-md-4 mb-4 text-center text-md-start">
                    <h6 class="fw-bold mb-3 text-uppercase">Theo dõi chúng tôi</h6>
                    <div class="mb-3">
                        <a href="https://www.facebook.com/sharer/sharer.php?u=<%= request.original_url %>" class="text-light fs-4 me-3"><i class="bi bi-facebook"></i></a>
                        <a href="https://www.instagram.com/sharer/sharer.php?u=<%= request.original_url %>" class="text-light fs-4 me-3"><i class="bi bi-instagram"></i></a>
                        <a href="https://twitter.com/intent/tweet?url=<%= request.original_url %>" class="text-light fs-4 me-3"><i class="bi bi-twitter"></i></a>
                    </div>
                    <p class="text-secondary small mb-0">Email: support@foodsdrinksapp.com</p>
                </div>
            </div>

            <hr class="border-secondary my-4">

            <div class="text-center text-secondary small">
                © <%= Time.current.year %> Foods & Drinks App — Thiết kế bởi <strong>Nguyen Phong</strong>.<br>
                © Công ty TNHH MMT.
            </div>
        </div>
    </footer>



    <% if user_signed_in? && !current_user.admin? %>
    <!-- Floating Suggestion Button -->
    <div id="suggestion-button">
        <button type="button" class="btn btn-primary rounded-circle" data-bs-toggle="modal" data-bs-target="#suggestionModal" title="Góp ý món ăn / thức uống">
            <i class="bi bi-envelope-fill"></i>
        </button>
    </div>

    <!-- Suggestion Modal -->
    <div class="modal fade" id="suggestionModal" tabindex="-1" aria-labelledby="suggestionModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="suggestionModalLabel">Góp ý món ăn / thức uống</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
                </div>
                <div class="modal-body">
                    <%= form_with(model: Suggestion.new, url: suggestions_path, local: true) do |f| %>
                    <div class="mb-3">
                        <%= f.label :title, "Tên món / thức uống", class: "form-label" %>
                        <%= f.text_field :title, class: "form-control", required: true %>
                    </div>

                    <div class="mb-3">
                        <%= f.label :content, "Mô tả / lý do đề xuất", class: "form-label" %>
                        <%= f.text_area :content, class: "form-control", rows: 4, required: true %>
                    </div>

                    <div class="text-end">
                        <%= f.submit "Gửi góp ý", class: "btn btn-success" %>
                    </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
    <% end %>
</body>

</html>