#!/bin/bash
set -e

echo "📦 Preparing database..."

# RUN MIGRATE + SEED EVERY START (SAFE)
bundle exec rails db:migrate 2>/dev/null || true
bundle exec rails db:seed 2>/dev/null || true

echo "🚀 Starting application..."

exec "$@"
