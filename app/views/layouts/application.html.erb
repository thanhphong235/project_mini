<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>Foods & Drinks App</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css" />

    <!-- Swiper JS -->
    <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>

    <style>
        /* ================= Navbar cố định trên cùng ================= */
        .navbar-custom {
            background-color: #000 !important;
            color: #fff !important;
        }

        .navbar-custom .nav-link,
        .navbar-custom .navbar-brand {
            color: #fff !important;
            position: relative;
            transition: color 0.3s;
        }

        .navbar-custom .nav-link::after {
            content: "";
            display: block;
            width: 0;
            height: 3px;
            background: #dc3545;
            transition: width 0.3s;
            position: absolute;
            bottom: 0;
            left: 0;
        }

        .navbar-custom .nav-link:hover::after {
            width: 100%;
        }

        .navbar-custom .nav-link:hover {
            color: #fff !important;
        }

        .navbar-custom .dropdown-menu {
            background-color: #111;
        }

        .navbar-custom .dropdown-item {
            color: #fff;
        }

        .navbar-custom .dropdown-item:hover {
            background-color: #dc3545;
            color: #fff;
        }

        .navbar-custom .btn-primary {
            background-color: #dc3545;
            border-color: #dc3545;
            color: #fff;
        }

        .navbar-custom .btn-primary:hover {
            background-color: #b71c1c;
            border-color: #b71c1c;
            color: #fff;
        }

        /* ================= Navbar fixed top ================= */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 2000;
            /* luôn nổi trên mọi thứ */
        }

        /* Padding cho main để không bị che bởi navbar */
        main {
            padding-top: 80px;
            /* bằng chiều cao navbar */
        }

        /* Nút góp ý */
        #suggestion-button {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 2000;
        }
    </style>
</head>

<body class="d-flex flex-column min-vh-100">

    <!-- Navbar -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-custom shadow-sm">
            <div class="container">
                <%= link_to "Foods & Drinks App", root_path, class: "navbar-brand fw-bold" %>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarContent">
                    <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("Home", root_path, class: "nav-link") :
                                link_to("Home", "#hero", class: "nav-link") %>
                        </li>
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("Menu", food_drinks_path, class: "nav-link") :
                                link_to("Menu", "#menu", class: "nav-link") %>
                        </li>
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("About", abouts_path, class: "nav-link") :
                                link_to("About", "#about", class: "nav-link") %>
                        </li>
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("Events", events_path, class: "nav-link") :
                                link_to("Events", "#events", class: "nav-link") %>
                        </li>
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("Chefs", chefs_path, class: "nav-link") :
                                link_to("Chefs", "#chefs", class: "nav-link") %>
                        </li>
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("Gallery", gallerys_path, class: "nav-link") :
                                link_to("Gallery", "#gallery", class: "nav-link") %>
                        </li>
                        <li class="nav-item">
                            <%= user_signed_in? ?
                                link_to("Contact", contacts_path, class: "nav-link") :
                                link_to("Contact", "#contact", class: "nav-link") %>
                        </li>
                    </ul>

                    <ul class="navbar-nav ms-auto align-items-center">
                        <% if user_signed_in? %>
                        <% if current_user.admin? %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle me-1"></i> <%= current_user.name || "Admin" %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><%= link_to "Dashboard", admin_dashboard_path, class: "dropdown-item" %></li>
                                <li><%= link_to "Profile", edit_user_registration_path, class: "dropdown-item" %></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><%= link_to "Đăng xuất", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Bạn chắc chắn muốn đăng xuất?" }, class: "dropdown-item text-danger" %></li>
                            </ul>
                        </li>
                        <% else %>
                        <li class="nav-item me-3">
                            <%= turbo_frame_tag "cart-count" do %>
                            <%= render "shared/cart_icon" %>
                            <% end %>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle me-1"></i> <%= current_user.name || "Tài khoản" %>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><%= link_to "Profile", edit_user_registration_path, class: "dropdown-item" %></li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><%= link_to "Đăng xuất", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Bạn chắc chắn muốn đăng xuất?" }, class: "dropdown-item text-danger" %></li>
                            </ul>
                        </li>
                        <% end %>
                        <% else %>
                        <li class="nav-item"><%= link_to "Log in", new_user_session_path, class: "nav-link" %></li>
                        <li class="nav-item"><%= link_to "Sign up", new_user_registration_path, class: "nav-link" %></li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <!-- Main content -->
    <main class="flex-grow-1">
        <div class="container mt-4">
            <%= turbo_frame_tag "flash_messages" do %>
            <%= render "shared/flash" %>
            <% end %>
            <%= yield %>
        </div>
    </main>

    <!-- Footer (giữ nguyên) -->
    <!-- Footer (giữ nguyên) -->
    <footer class="bg-dark text-light py-3 mt-auto">
        <div class="container">
            <div class="row text-center text-md-start">
                <div class="col-md-4 mb-4">
                    <h5 class="fw-bold mb-3">🍴 Foods & Drinks App</h5>
                    <p class="text-secondary">Ứng dụng giúp bạn khám phá, đặt món ăn và đồ uống yêu thích mọi lúc, mọi nơi.</p>
                </div>
                <div class="col-md-4 mb-4">
                    <h6 class="fw-bold mb-3 text-uppercase">Liên kết nhanh</h6>
                    <ul class="list-unstyled">
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Trang chủ</a></li>
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Về chúng tôi</a></li>
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Chính sách bảo mật</a></li>
                        <li><a href="#" class="text-secondary text-decoration-none d-block mb-2">Điều khoản sử dụng</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-4 text-center text-md-start">
                    <h6 class="fw-bold mb-3 text-uppercase">Theo dõi chúng tôi</h6>
                    <div class="mb-3"> <a href="#" class="text-light fs-4 me-3"><i class="bi bi-facebook"></i></a> <a href="#" class="text-light fs-4 me-3"><i class="bi bi-instagram"></i></a> <a href="#" class="text-light fs-4 me-3"><i class="bi bi-twitter"></i></a> </div>
                    <p class="text-secondary small mb-0">Email: support@foodsdrinksapp.com</p>
                </div>
            </div>
            <hr class="border-secondary my-4">
            <div class="text-center text-secondary small"> © <%= Time.current.year %> Foods & Drinks App — Thiết kế bởi <strong>Nguyen Phong</strong>.<br> © Công ty TNHH MMT. </div>
        </div>
    </footer>

    <% if user_signed_in? && !current_user.admin? %>
    <!-- Floating Suggestion Button -->
    <div id="suggestion-button">
        <button type="button" class="btn btn-primary rounded-circle" data-bs-toggle="modal" data-bs-target="#suggestionModal" title="Góp ý món ăn / thức uống">
            <i class="bi bi-envelope-fill"></i>
        </button>
    </div>

    <!-- Suggestion Modal -->
    <div class="modal fade" id="suggestionModal" tabindex="-1" aria-labelledby="suggestionModalLabel" aria-hidden="true">
        ...
    </div>
    <% end %>

</body>

</html>