<% provide(:title, "Quên mật khẩu") %>

<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card shadow-lg border-0 rounded-4 p-4">
            <div class="text-center mb-4">
                <h2 class="fw-bold">Forgot password?</h2>
                <p class="text-muted">Enter your email to reset password.</p>
            </div>

            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { class: "needs-validation" }) do |f| %>
            <div class="mb-3">
                <%= f.label :email, "Email", class: "form-label fw-semibold" %>
                <%= f.email_field :email, required: true, autofocus: true, class: "form-control form-control-lg rounded-3", placeholder: "Enter your email" %>
            </div>

            <div class="d-grid mb-3">
                <%= f.submit "Send me reset instructions", class: "btn btn-primary btn-lg rounded-3 fw-bold" %>
            </div>
            <% end %>

            <p class="text-center mt-3">
                <%= link_to "Back to login", new_session_path(resource_name), class: "text-primary fw-semibold" %>
            </p>
        </div>
    </div>
</div>