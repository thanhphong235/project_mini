<h2>Chỉnh sửa danh mục</h2>

<div class="row justify-content-center mt-4">
    <div class="col-md-6 col-lg-5">
        <div class="card shadow-sm p-3">
            <%= render 'form' %>
            <div class="mt-3">
                <%= link_to "Quay lại", admin_categories_path, class: "btn btn-outline-secondary" %>
            </div>
        </div>
    </div>
</div>